PROGRAMS = \
	test_json_parser \
	test_ascii_filter \
	test_qprint_filter \
	test_base64_filter \

.DEFAULT: $(CXXBuild $(PROGRAMS))
	$(RunTest)

clean:
        rm -rf $(filter-proper-targets $(ls R, .)) *.omc
