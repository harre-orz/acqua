.DEFAULT: $(CXXProgram test_pointer_container_iterator, test_pointer_container_iterator) \
          $(CXXProgram test_sequenced_map, test_sequenced_map)

clean:
        rm -rf $(filter-proper-targets $(ls R, .)) *.omc
