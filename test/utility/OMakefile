PROGRAMS = \
	test_move_on_copy_wrapper \
	test_string_cast \
	test_backtrace \
	test_hexdump \

.DEFAULT: $(CXXBuild $(PROGRAMS))
	$(RunTest)

clean:
        rm -rf $(filter-proper-targets $(ls R, .)) *.omc
