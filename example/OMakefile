INCLUDES += ../include/
CXXFLAGS += -g

PROGRAMS = \
	interfaces \
	inotify_listener \
	netlink_listener \
	fcopy_server \
	fcopy_client \

.DEFAULT: $(CXXBuild $(PROGRAMS))

clean:
        rm -rf $(filter-proper-targets $(ls R, .)) *.omc
