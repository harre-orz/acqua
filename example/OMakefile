INCLUDES += ../include/
CXXFLAGS += -g

PROGRAMS = \
	thttpproxy \
	interfaces \
	ping \
	inotify_listener \
	netlink_listener \

.DEFAULT: $(CXXBuild $(PROGRAMS))

clean:
        rm -rf $(filter-proper-targets $(ls R, .)) *.omc
